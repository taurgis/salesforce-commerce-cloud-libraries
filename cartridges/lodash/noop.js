'use strict';

/**
 * This method returns `undefined`.
 *
 * @static
 * @since 2.3.0
 * @category Util
 * @example
 *
 * times(2, _.noop); => [undefined, undefined]
 */
function noop() {
    // No operation performed.
}

module.exports = noop;
